// from code.esm.sh
import React from "react";
import {createRoot} from "react-dom";
import App from "./basic_app.tsx";

const root = createRoot(document.getElementById("root"));
root.render(<App />);

